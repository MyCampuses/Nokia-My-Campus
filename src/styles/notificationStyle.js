//import {makeStyles} from '@material-ui/core';

const notificationStyles = () => {
  const update = {
    position: 'absolute',
    left: '50%',
    top: '50%',
  };

  return {
    update: update,
  };
};

export default notificationStyles();