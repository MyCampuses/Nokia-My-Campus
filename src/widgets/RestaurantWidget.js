import DonutFragment from "./DonutFragment";

const RWidget = (props) =>{
    const {Donut} = DonutFragment();

    const RDWidget = (items) =>{
        return (
            <Fragment>

            </Fragment>
        );
    };
    return {
        RDWidget: RDWidget
    }
};