/*
Still under construction, it's supposed to be part of edit button,
when pressed it will enable the mode where you can modify widgets.
*/
export const editmode = (value) => {
    return {
        type: 'EDITMODE',
        value
    }
};